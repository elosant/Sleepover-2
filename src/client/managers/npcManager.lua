-- NOTES:
-- must only have one instance of an npc of type characters
-- Services
local serverStorage = game:GetService("ServerStorage")

local entities = {}

local NpcManager = {}

function NpcManager.init()
end

function NpcManager.update()
end

function NpcManager.createNpc(npcType, initialState)

end

return NpcManager
